## Packages
framer-motion | Page transitions and scroll animations
embla-carousel-react | Testimonials carousel
lucide-react | Icons (already in base but ensuring availability)
zod | Schema validation
react-hook-form | Form handling
@hookform/resolvers | Zod resolver for forms

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  heading: ["Plus Jakarta Sans", "sans-serif"],
}
Colors: Sage Green, Terracotta, Cream
Backend API: /api/inquiries (POST), /api/testimonials (GET)
